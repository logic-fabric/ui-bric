import { css } from 'styled-components'

export const buttonStyles = css`
    display: flex;
    gap: 1em;

    width: fit-content;
    padding: 0.5em 1em;
    border-radius: 4px;

    background-color: #eee;
    cursor: pointer;
`